<h1>User Settings</h1>
<h3>Password</h3>
<%= link_to "Change Password", change_password_user_settings_path, class: 'btn btn-primary settings' %>

<h3>Email Subscriptions</h3>
<% if @user.subscription %>
  You are currently subscribed to monthly update emails, <%= link_to "Unsubscibe", unsubscribe_user_settings_path(@user), class: 'btn btn-primary delete' %>
<% else %>
  You are currently unsubscribed to monthly update emails, <%= link_to "Subscibe", subscribe_user_settings_path(@user), class: 'btn btn-primary' %>
<% end %>

<h3>Premium Membership</h3>
<% if @user.package && @user.package.classification == "Premium" %>
  You have the Premium Package, downgrade to the Standard Package: <%= link_to "Downgrade to Standard", downgrade_user_settings_path, class: 'btn btn-primary delete' %>
<% else %>
  Upgrade to the smartXchange Premium Package: <%= link_to "smartXchange Premium", new_transaction_path, class: 'btn btn-primary premium' %>
<% end %>

<h3>Active</h3>
<% if @user.active %>
  You are currently browsing in active mode, switch to inactive mode? <%= link_to "Inactive", deactivate_user_settings_path, class: 'btn btn-primary delete' %>
<% else %>
  You are currently browsing in inactive mode, switch to active mode? <%= link_to "Active", activate_user_settings_path, class: 'btn btn-primary settings' %>
<% end %>
