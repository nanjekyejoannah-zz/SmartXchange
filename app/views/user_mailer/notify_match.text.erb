Dear <%= @user.name %>,
===============================================

<%= image_tag(attachments["#{@user.name}.jpg"].url, style: "padding-left:10px;", alt: "#{@user.name}") %>
<%= image_tag(attachments["linkedin.png"].url, alt: "Linkedin") if @user.linkedin %>
  <%= "#{@user.name}" %>, <%= "#{@user.title}, wants to start #{['a','e','i','o','u'].include?(@user.language[0].downcase) ? 'an' : 'a'} #{@user.language} conversation with you" %>
  <%= link_to "View Profile", @url_user, class: 'mail-btn' %>
