<!-- Navigation -->
<nav class="navbar navbar-default navbar-fixed-top">
    <div class="container-header">
      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header page-scroll">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#smartxchange">
              <span class="sr-only">Toggle navigation</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
        <a class="navbar-brand" href="<%= root_url %>"><%= image_tag('logo.png', alt: 'SmartXchange') %></a>
      </div>

      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse" id="smartxchange">
        <ul class="nav navbar-nav navbar-right">
          <% if signed_in? %>
            <li id="board-header"><%= link_to "Board#{@unread_posts}", board_url %> </li>
            <li><%= link_to "Network", users_url %></li>
            <li id="chat-rooms-header"><%= link_to "Conversations#{@unread_chat_rooms}", chat_rooms_url %></li>
            <li><%= link_to "My profile", user_url(current_user) %></li>
            <li><%= link_to "About", about_url %></li>
            <li><%= link_to "Log out", session_url, method: :delete %></li>
          <% else %>
            <li><%= link_to "Log in", login_url %></li>
            <li><%= link_to "Sign up", signup_url %></li>
          <% end %>
        </ul>
      </div>
      <!-- /.navbar-collapse -->
  </div>
  <!-- /.container-fluid -->
</nav>
