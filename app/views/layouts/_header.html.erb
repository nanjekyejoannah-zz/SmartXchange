<nav class="navbar navbar-default" role="navigation">
  <div class="container container-fluid">
    <div class="navbar-header">
      <% if signed_in? %>
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#chairshareapp">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <div id="div-item-data" data-item-id="<%= current_user.id %>"></div>
      <% end  %>
      <a class="navbar-brand" href="/">SmartXchange</a>
    </div>

    <div class="collapse navbar-collapse" id="chairshareapp">
      <ul class="nav navbar-nav">
      <% if signed_in? %>
        <li><%= link_to "My profile", user_url(current_user) %></li>
        <li><%= link_to "Home", root_url %></li>
        <!-- refactor this -->
        <% if count_unread > 0 %>
          <li class="chats-header"><%= link_to "Chats (#{count_unread})", chats_url %></li>
        <% else %>
          <li class="chats-header"><%= link_to "Chats", chats_url %></li>
        <% end %>
        <li><%= link_to "About", about_url %></li>
        <li><%= link_to "Log out", session_url, method: :delete %></li>
      <% else %>
        <li><%= link_to "Log in", login_url %></li>
        <li><%= link_to "Sign up", signup_url %></li>
      <% end %>
      </ul>
    </div>

  </div>
</nav>

<% if flash %>
  <% flash.each do |message_type, message| %>
          <div class="alert alert-<%= message_type %>"><%= message %></div>
  <% end %>
<% end %>
