<!DOCTYPE html>
<html>
<head>
  <title>SmartXchange</title>
  <link href="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css" rel="stylesheet">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <meta content='<%= signed_in? ? current_user.id : "" %>' name='user-id'/>
  <%= csrf_meta_tags %>

</head>
<body>
    <%= render partial: '/layouts/header', :locals => { :count_unread => user_count_unread(current_user) } %>
    <div class="container">
      <%= yield %>
      <%= render partial: '/layouts/footer' %>
      <%= debug(params) if Rails.env.development? %>
    </div>
    <audio id="chatAudio"><source src="/sounds/decay.mp3" type="audio/mpeg"></audio>
</body>
</html>
